import { StyleSheet, css } from 'aphrodite';

export const styleApphr = StyleSheet.create({
    linkStyle: {
        color: '#4f81bd',
        fontSize: '14px',
        cursor: 'pointer',
        ':hover': {
            color: '#093972',
        }
    }
});